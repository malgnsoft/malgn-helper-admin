{
  "name": "malgn-helper-admin",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt build && wrangler pages deploy .output/public --project-name malgn-helper-admin"
  },
  "devDependencies": {
    "nuxt": "^3.13.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
